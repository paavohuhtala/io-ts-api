{
  "name": "io-ts-api",
  "version": "0.1.0",
  "license": "MIT",
  "private": true,
  "scripts": {
    "dev:example": "yarn workspace io-ts-api-express-todo dev",
    "dev": "tsc --build --watch"
  },
  "workspaces": [
    "packages/*"
  ]
}
